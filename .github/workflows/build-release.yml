name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g. v2.1.87-dev)'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Create @ant stub packages
        run: |
          mkdir -p node_modules/@ant/computer-use-mcp
          cat > node_modules/@ant/computer-use-mcp/package.json << 'EOF'
          { "name": "@ant/computer-use-mcp", "version": "0.0.0", "main": "index.js" }
          EOF
          cat > node_modules/@ant/computer-use-mcp/index.js << 'EOF'
          export function buildComputerUseTools() { return []; }
          export function createComputerUseMcpServer() { return null; }
          export const API_RESIZE_PARAMS = {};
          export const targetImageSize = 768;
          export function bindSessionContext() {}
          export const DEFAULT_GRANT_FLAGS = [];
          EOF
          cat > node_modules/@ant/computer-use-mcp/types.js << 'EOF'
          export const DEFAULT_GRANT_FLAGS = {};
          EOF
          cat > node_modules/@ant/computer-use-mcp/sentinelApps.js << 'EOF'
          export function getSentinelCategory() { return null; }
          EOF

          mkdir -p node_modules/@ant/computer-use-input
          cat > node_modules/@ant/computer-use-input/package.json << 'EOF'
          { "name": "@ant/computer-use-input", "version": "0.0.0", "main": "index.js" }
          EOF
          cat > node_modules/@ant/computer-use-input/index.js << 'EOF'
          export const isSupported = false;
          EOF

          mkdir -p node_modules/@ant/computer-use-swift
          cat > node_modules/@ant/computer-use-swift/package.json << 'EOF'
          { "name": "@ant/computer-use-swift", "version": "0.0.0", "main": "index.js" }
          EOF
          cat > node_modules/@ant/computer-use-swift/index.js << 'EOF'
          export {};
          EOF

          mkdir -p node_modules/@ant/claude-for-chrome-mcp
          cat > node_modules/@ant/claude-for-chrome-mcp/package.json << 'EOF'
          { "name": "@ant/claude-for-chrome-mcp", "version": "0.0.0", "main": "index.js" }
          EOF
          cat > node_modules/@ant/claude-for-chrome-mcp/index.js << 'EOF'
          export const BROWSER_TOOLS = [];
          export function createClaudeForChromeMcpServer() { return null; }
          EOF

      - name: Build Windows .exe
        run: bun run scripts/build.ts --compile --windows

      - name: Build Linux binary
        run: bun run scripts/build.ts --compile

      - name: Build JS bundle
        run: bun run scripts/build.ts

      - name: Package JS bundle
        run: |
          mkdir -p dist-bundle
          cp -r dist-js dist-bundle/
          cp package.json dist-bundle/

      - name: Determine version
        id: version
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            echo "tag=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: Release ${{ steps.version.outputs.tag }}
          body: |
            ## Windows 用户
            - **Windows 10/11**: 下载 `cli.exe` 直接运行（无需安装 Bun）
            - **Windows 7**: Bun 编译的 exe 依赖 `RtlGetSystemTimePrecise`（Win8+），不兼容 Win7。请使用 JS bundle：
              1. 下载 `cli-js-bundle.zip` 并解压
              2. 安装 [Node.js 18+](https://nodejs.org/) 或 [Bun 1.0.x](https://bun.sh/)
              3. 运行 `node cli.js --version` 或 `bun cli.js --version`

            ## Linux 用户
            下载 `cli` 并 `chmod +x cli && ./cli --version`
          files: |
            dist/cli.exe
            dist/cli
            dist-js/cli.js
            dist-js/README-15xfep0j.md
            dist-js/README-a20jqpfq.md
            dist-js/README-fdkrkffh.md
            dist-js/README-sts9jh7p.md
            dist-js/SKILL-dw2s725q.md
            dist-js/batches-jj4gxxrq.md
            dist-js/batches-z6rg4c5n.md
            dist-js/claude-api-akprmxgv.md
            dist-js/claude-api-b5fb83xf.md
            dist-js/claude-api-ckh5vv5d.md
            dist-js/claude-api-t3fc2xy0.md
            dist-js/claude-api-ycaa4za1.md
            dist-js/error-codes-gbkf4wcz.md
            dist-js/examples-t0ram870.md
            dist-js/files-api-vqrrfqpr.md
            dist-js/files-api-y1jg4bfp.md
            dist-js/live-sources-jsnc9cqd.md
            dist-js/models-5393x8e2.md
            dist-js/patterns-gyzc7sqy.md
            dist-js/patterns-vht8ez3b.md
            dist-js/prompt-caching-4tyw4ek1.md
            dist-js/streaming-q5mkr0fz.md
            dist-js/streaming-ysbat3vy.md
            dist-js/tool-use-concepts-s3a15sft.md
            dist-js/tool-use-dfasx2c3.md
            dist-js/tool-use-he8fpgww.md
            package.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
